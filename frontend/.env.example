# Browser-facing URL of the FastAPI extraction service.
# Local development defaults to this same address when the variable is absent.
VITE_API_BASE_URL=http://127.0.0.1:7860
# Set to gradio when calling a public Gradio Space instead of the FastAPI API.
VITE_API_TRANSPORT=rest
